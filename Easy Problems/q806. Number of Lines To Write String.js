var numberOfLines = function(widths, s) {
    let result = [];

    let sumWidths = 0;
    let countS = s.length;

    for(let i = 0;i < widths.length;i++) {
        sumWidths+= widths[i];
    }

    while(s < 10) {
        
    }
    
};